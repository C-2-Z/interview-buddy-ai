# Codex 未完成任务清单与处理结果

> 生成时间：2026-07-17
> 依据：仓库内 `codex/*` 分支、工作区未提交变更、以及本次要求的两项专项任务。

Codex 在本项目中并没有单独的"任务清单文件"，其未完成工作以 **本地分支** 和 **未提交的工作区改动** 的形式存在。逐一枚举如下。

## 一、Codex 记录的未完成任务（分支/工作区）

| # | 来源 | 内容 | 状态（本次处理前） | 处理结果 |
|---|------|------|--------------------|----------|
| 1 | `codex/docs-reorganization`（当前分支工作区，未提交） | Monorepo 整合：删除遗留 `api-server/` 与根 `src/`、`src-tauri/`，重构为 `apps/{web,api,desktop}` 的 npm workspaces 结构 | 改动未提交，处于半完成状态 | 已提交并作为合并基线 |
| 2 | `codex/fix-review-safety-boundaries`（15 个提交） | 安全与隐私加固：agent_events 移除回答正文、日志脱敏加固、知识库流式错误隐藏、事件元数据边界收紧、3 个隐私迁移与测试门禁 | 编辑的是"已被整合删除"的旧 `api-server/`，与整合分支直接冲突 | 已移植到 `apps/api` / `apps/web` 并合入 |
| 3 | `codex/temp-vconsole` | 临时 vConsole 调试分支 | 领先 main 0，落后 28，无独有提交 | 判定为废弃，不合并 |
| 4 | `csc` | 历史开发分支 | 领先 main 0，落后 97，无独有提交 | 已并入 main，无需合并 |
| 5 | `cys` | 历史开发分支 | 领先 main 0，落后 28，无独有提交 | 已并入 main，无需合并 |

### 任务 2 的 15 个提交明细（安全加固）

- `e40bc52` fix(agent): 移除事件中的回答正文
- `249806a` / `323738b` fix(agent): 保留事件扩展字段与运行时契约
- `8508c48`/`f6b4bec`/`a36676c`/`afcb80b`/`dbcc9cc` fix(logging): 日志脱敏系列加固（恢复安全输出、封堵旁路、非普通对象凭据泄漏、代理对象降级、语音错误元数据延迟脱敏）
- `ebc1c70` fix(report): 忽略未观察维度评分
- `88cf4e0`/`b03bb9d` fix(knowledge): 隐藏流式问答内部错误、安全处理任意流式异常
- `a7f678c` fix(agent): 限制事件元数据边界
- `f20d933` db(agent): 分阶段应用事件隐私迁移
- `8bfee5e`/`4d24205` test: 增加 TypeScript 与回归门禁、修复异常流测试静态检查

## 二、本次要求的两项专项任务

| # | 任务 | 交付物 | 状态 |
|---|------|--------|------|
| A | 使用 `@superpowers` 方法论对本项目进行全面代码审查 | `docs/code-review-report.md` | 已完成 |
| B | 使用 `Leonxlnx/taste-skill` 生成答辩演示交互式网页 | `docs/project-showcase.html` | 已完成 |

## 三、合并策略结论

- 有效需要合并的只有 **任务 1（整合）** 与 **任务 2（安全加固）**；其余分支无独有提交。
- 由于任务 2 编辑的旧目录已被任务 1 删除，采用"**路径重写 + 三方补丁**"方式，将安全修复精确移植到新的 `apps/api` 与 `apps/web`，并保留 3 个 Supabase 隐私迁移。
- 合并后全部类型检查、生产构建与相关静态/契约测试通过（详见合并说明）。
