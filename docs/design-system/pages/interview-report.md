# Interview report override

- Lead with the overall score and concise AI summary, then show per-question evidence.
- Use existing data only: overall score, per-question score, answer, and feedback.
- Cards must avoid print breaks. Navigation and actions are hidden in print output.
- Missing or partial scores display an explicit neutral state rather than zero-filled charts.
